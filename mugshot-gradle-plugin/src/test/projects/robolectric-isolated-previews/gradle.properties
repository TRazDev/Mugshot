# The point of the fixture: the generated screenshot test moves to its own task and JVM, so
# layoutlib never loads alongside Robolectric's instrumented android.* classes.
uk.co.fractalmotion.mugshot.isolateTests=true
