rootProject.name = 'build-logic'

include(':aar2jar')
include(':mugshot-gradle-plugin')
project(':mugshot-gradle-plugin').projectDir = new File('../mugshot-gradle-plugin')

dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files("../gradle/libs.versions.toml"))
    }
  }
}
